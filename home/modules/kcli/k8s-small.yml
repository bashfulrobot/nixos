   cp01:
     image: ubuntu2204
     numcpus: 4
     memory: 4096
     disks:
        - size: 60
     packages:
     - git
     - neovim
     - curl
     cmds:
     - apt update && apt dist-upgrade -y
     - date > dk-run.log
